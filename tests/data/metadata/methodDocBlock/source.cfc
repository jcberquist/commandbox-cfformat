component {

    /**
    * checks for the default image and default shell
    * @abc is a parameter
    * @abc.def random
    * @abc.required true
    * @abc.type string
    */
    function test(abc) {

    }

}
